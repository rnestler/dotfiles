#!/bin/bash

date=$(date +%Y-%m-%d) 
check=$(grep $date ~/Documents/Termine.txt)
if [ "x$check" == "x" ];  then
	echo nothing
else
	notify-send Reminder "$(echo $check | awk '{print $3" "$4" "$5" "$6}')"
fi
	
