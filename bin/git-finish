#!/bin/bash

set -euo pipefail

current_branch=$(git rev-parse --abbrev-ref HEAD)
git fetch --all --prune
git rebase -i origin/master
git checkout master
git merge --ff-only
git branch -d "${current_branch}"
