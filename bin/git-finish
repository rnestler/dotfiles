#!/bin/bash

set -euo pipefail

current_branch=$(git rev-parse --abbrev-ref HEAD)
git fetch --all --prune
git checkout master
git merge --ff-only
git checkout "${current_branch}"
git rebase -i master
git checkout master
git branch -d "${current_branch}"
